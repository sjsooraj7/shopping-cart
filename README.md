# React + TypeScript + Vite

Create react project using the command
pnpm create vite shopping-cart --template react-ts

Add the repo to the github

Vite is our development/build tool.

vite Vite gives Development server, Fast reload, TypeScript support, Production build

Install the required packages
-Zustand
-Zod
-Bootstrap
-@tanstack/react-query